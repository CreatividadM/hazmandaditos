import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.creatividadmovil.hazmandaditos',
  appName: 'Hazmandaditos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
